    .compile(trx.dialect())
    .stream(trx)
    .map(row -> br.com.objectos.way.schema.it.SalaryOrm.get(orm).load(br.com.objectos.way.schema.it.EmployeePojo.this, row))
    .collect(br.com.objectos.core.collections.MoreCollectors.toImmutableList());